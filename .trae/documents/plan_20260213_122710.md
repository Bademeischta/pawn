Ich werde den Plan um die von dir geforderten Punkte (MCTS Memory/Math, SumTree-Logik, ML-Balance) erweitern und die Umsetzung starten.

### Phase 1: Executive Summary & Audit Report
- **Umfassende Analyse**: Ich liefere eine detaillierte Bewertung des Ist-Zustands, fokussiert auf Architektur, Skalierbarkeit und ML-Integrität.

### Phase 2: Implementation & Fixes (Erweitert)

1.  **`start.ps1` (NEU)**:
    - Natives PowerShell-Skript für Windows mit automatischer `venv`-Erstellung, Pfad-Validierung und Abhängigkeits-Check.
2.  **`start.sh` (REFACTOR)**:
    - POSIX-konformes Skript mit robuster Fehlerbehandlung.
3.  **`distillzero_factory.py` (HARDENING)**:
    - Robuste `multiprocessing`-Logik für Windows.
    - Sichere HDF5-Schreibvorgänge (Atomic Flushes) und Download-Validierung.
4.  **`mcts.py` (CORE LOGIC & MEMORY)**:
    - **Math-Check**: Verifizierung der PUCT-Implementierung.
    - **Dirichlet-Noise**: Korrekte Anwendung am Root-Knoten sicherstellen.
    - **Garbage Collection**: Implementierung einer `prune()` Methode oder eines strikteren Memory-Managements im `TranspositionTable`, um unendliches Wachstum zu verhindern (OOM-Schutz).
5.  **`sumtree.py` (DATA STRUCTURE)**:
    - **Audit**: Tiefenprüfung auf Off-by-One Fehler bei Indizes und Prioritäts-Updates. Korrektur falls notwendig.
6.  **`model.py` & `train_end_to_end.py` (ML CONFIG)**:
    - **Loss Balancing**: Überprüfung und Kalibrierung der Gewichtung zwischen Policy- und Value-Loss.
    - **Regularisierung**: Sicherstellung, dass L2 Weight Decay im Optimizer (`AdamW`) korrekt greift.
    - **Zombie-Schutz**: `try...finally` Blöcke für externe Engines in der Trainingsschleife.
7.  **`dashboard.py` (ROBUSTNESS)**:
    - Graceful Degradation bei fehlenden Daten/Modellen.
8.  **`metrics.py` (PERFORMANCE)**:
    - Aktivierung des SQLite WAL-Modus (Write-Ahead Logging) für High-Concurrency.
9.  **`utils.py` (SAFETY)**:
    - Umstellung auf `pathlib` und `safe_save` Logik.

Ich beginne sofort mit der Generierung der Lösungen.